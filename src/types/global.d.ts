declare global {
  interface Window {
    // Any global window properties can be added here as needed
  }

  interface Document {
    // Any global document properties can be added here as needed
  }
}

export {};
