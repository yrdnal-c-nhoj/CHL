# TODO

- [x] Fix syntax error in `src/pages/2026/26-05/26-05-30/Clock.tsx` inside `assetConfig` (invalid `bgLayer:` + spread).
- [x] Verify `useMultiAssetLoader` assetConfig format by removing `bgLayer` line and making the object entries valid.
- [ ] Run `npm run dev` to confirm Vite no longer returns 500 for that page.


