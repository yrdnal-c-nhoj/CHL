# TODO
- [ ] Refactor `src/styles/ClockList.module.css` to streamline CSS and reduce duplication.
- [ ] Keep shared tag styling in `src/styles/globals.css` (do not modify it).
- [ ] Verify mobile/desktop layout behavior after CSS refactor.
- [ ] Run `npm test` and/or `npm run build` (whichever is available) to ensure no regressions.

