# TODO
- [ ] Update Clock.tsx to render 8 digits as a 2-row x 4-column grid.
- [ ] Update Clock.tsx inline styles (or switch to CSS module usage) so the grid layout is applied.
- [ ] Verify build/tests pass.

